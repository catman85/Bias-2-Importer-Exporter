// ATTENTION
// npm install bootstrap-vue bootstrap-css-only
// ATTENTION
// also add in package.json otherwise the npm run build will brake
// "optionalDependencies": {
//     "jquery": "1.9.1 - 3"
//  },

import Vue from "vue";
import BootstrapVue from "bootstrap-vue";

Vue.use(BootstrapVue);
